# Relay Desk (mock)

Tiny mock project used to showcase BMAD technical research discovered by [[specwiki]].

**Idea:** a local-first CLI that turns standup notes into a shared status board.

The research artifacts live under `_bmad-output/`. Run:

```bash
npx specwiki generate --project examples/bmad-research-relay
npx specwiki open --project examples/bmad-research-relay
```
